using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//
// Intro - Battle in the Forest:
// this script controls the setup of the main battle occuring in the forest
// e.g.: the geographic zone of the enemies, when all enemies are defeated, ...
//
public class BattleForestController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
